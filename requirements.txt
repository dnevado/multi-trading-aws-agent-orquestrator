numpy>=1.24
pandas>=2.2
yfinance>=0.2.27
# Strands is a project-specific package that provides Agent/Graph tooling used in the demo.
# Install from your internal index or replace with the appropriate package name/version.
strands>=0.1.0

# Optional / dev
pytest>=7.0